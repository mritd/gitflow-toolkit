# Charm v2 implementation plan

> Status (2026-09-10): Migration deferred and its code/dependency changes rolled
> back to the existing v1 stack with user approval. The verification record below
> is historical: it did not cover actual screen erasure across sequential TUI
> programs. A later tmux reproduction exposed stale inline frames caused by the
> pinned Ultraviolet renderer's cursor handling during shrink. Terminal-query
> shutdown research and the separate Bubble Tea fork remain preserved.

## Rollback verification (2026-09-10)

- Restored tracked source, tests, go.mod, and go.sum exactly to the pre-migration
  HEAD. Removed the migration-only v2 test file; preserved both migration documents
  and the separate Bubble Tea fork.
- `go test -race ./...` and `go build ./...`: passed.
- `golangci-lint run --max-same-issues 0 --max-issues-per-linter 0 ./...`: 0 issues.
- An isolated tmux test rendered the actual selector, AI generation, and AI
  preview views with controlled completion, without invoking LLM or Git actions.
  All three intermediate views cleared; only the final result remained. The
  temporary test and binary were removed after verification.
- Installed executables were not replaced; reinstall the restored v1 build
  separately. No system-directory writes, commits, or pushes were performed.

**Goal:** Upgrade the Charm stack without changing Git workflows or terminal behavior.

**Architecture:** Migrate application models to v2 views/events, preserve composed Bubbles string views and virtual cursors, retain adaptive colors through the upstream compatibility package, and adapt standalone output at the writer boundary.

**Stack:** Go 1.27.1, Bubble Tea v2.0.9, Bubbles v2.2.1, Lip Gloss v2.0.6.

## Tasks

1. Add the v2 modules and regression tests using v2 key/paste messages. Run them against existing models and confirm the missing handlers fail.
2. Migrate imports, model views, key events, adaptive colors, and test event construction. Migrate textinput styles/width/cursor mode and list defaults. Run `go test ./internal/ui/...`.
3. Adapt standalone styled output in `cmd` to output-aware writers, including Cobra's version template. Verify redirected success/error output has no ANSI escapes. Run `go test ./cmd`.
4. Tidy modules, format changed Go files, and verify `go test -race ./...`, `go build ./...`, and `golangci-lint run --max-same-issues 0 --max-issues-per-linter 0 ./...`. Run harmless PTY and redirected CLI smoke checks.
5. Run one final adversarial subagent review. Personally validate findings, fix genuine issues, rerun affected checks, and hand off uncommitted changes.

## Execution constraints

Work in the user-provided checkout and preserve unrelated changes. Do not stage, commit, push, install aliases, invoke a real AI service, or perform real Git mutations in smoke tests. Apply the user's authorized implementation workflow without additional plan approval or per-task review gates.

## Verification record

- V2 selector navigation and input editing tests failed against the v1 handlers, then passed after migration.
- Redirected push success/error output tests caught ANSI escapes after the v2 renderer change, then passed with output-aware writers.
- The light-theme pagination test caught cached dark dots; both theme cases pass after setting the paginator strings explicitly.
- `go test -race ./...`: passed.
- `go build ./...`: passed.
- `golangci-lint run --max-same-issues 0 --max-issues-per-linter 0 ./...`: 0 issues.
- `git diff --check`: passed.
- `go run . --version` through a pipe: readable text with no ANSI escape bytes.
- Manual selector PTY smoke: displayed the inline list, selected FIX with down/enter, and restored cursor/bracketed-paste state. The tool PTY reports zero dimensions by default, so the temporary smoke harness supplied 80x24; the helper was removed afterward.
- The permanent `TestSelector_ProgramInput` also exercises the real v2 terminal byte decoder without any Git operation.
- Final adversarial review: no blocking findings. Strengthened the pagination test to detect removal of the whole theme initialization block; deleting that block made the test fail, and restoring it made the test pass.
- Reviewer also verified `go mod tidy -diff`, `go mod verify`, and uncached UI/command tests. Targeted race tests and lint passed after the test improvement.
